# brightness
# ```sudo apt install -y brightnessctl```
# ```sudo usermod -a -G video ${USER}``` and relogin again
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set +5% && $refresh_i3bar
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 5%- && $refresh_i3bar