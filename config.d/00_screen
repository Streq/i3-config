# Setup picom
# ```mkdir -p ~/.config/picom/ ```
# ```cp ~/.config/i3/picom/picom.conf ~/.config/picom/picom.conf ```
# Setup nitrogen
# ```sudo apt install -y nitrogen```

# start picom(compositor), the monitor layout(script setting up double monitors), and nitrogen, in that order
exec --no-startup-id picom -b && monitor_setup && nitrogen --restore
